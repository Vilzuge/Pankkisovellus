<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pankkisovellus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ChangeAccountInformation"></activity>
        <activity android:name=".activity.AddCard" />
        <activity android:name=".activity.AccountInformation" />
        <activity android:name=".activity.DepositOrWithdraw" />
        <activity android:name=".activity.NewPayment" />
        <activity android:name=".activity.CreateNewAccount" />
        <activity android:name=".activity.Dashboard" />
        <activity android:name=".activity.ChangeUserInformation" />
        <activity android:name=".activity.CreateNewUser" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>